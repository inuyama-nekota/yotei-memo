# 予定メモ v2.3

予定を大きく、簡単に確認するためのスマホ向けアプリです。

## 主な機能

- 場所・日付・時間・備考だけの簡単入力
- 日付はカレンダー選択
- 時間は時と分を選択、分は5分単位
- 近い予定から自動で並び替え
- 1〜3日以内はピンク、4〜7日以内は黄色、それより先は水色
- 予定時刻を過ぎた予定は最下部へ移動し、薄く表示
- 過去の予定は削除ボタン以外を操作できない表示
- 端末内へ自動保存
- オフライン利用
- iPhone・Androidのホーム画面へ追加可能

## v2.2 の調整

- スマホ基準レイアウトへ調整
- 予定カードを3段構成へ変更
  - 1段目: 場所
  - 2段目: 日付 / 時間
  - 3段目: 備考
- タイトルと新規作成ボタンの位置ずれを修正
- ヘッダー凡例を横1列に整理
- カード内の上下余白を圧縮
- 時間欄の右余白を削減
- 削除ボタンを右上の丸型にはみ出し配置へ変更

## Macで動作確認

1. ZIPを展開します。
2. `予定メモを起動.command` を開きます。
3. 初回の警告では、右クリックして「開く」を選びます。
4. ブラウザで `http://127.0.0.1:8772` が開きます。

終了時はターミナルで `Control + C` を押します。

## GitHub Pagesで公開

このフォルダの中身をGitHubリポジトリの `main` ブランチへ置きます。
同梱のGitHub Actionsが公開処理を行います。

リポジトリの Settings → Pages → Build and deployment で
Source を `GitHub Actions` にしてください。

## スマホへ追加

### iPhone
Safariで公開URLを開き、
共有ボタン → `ホーム画面に追加` → `追加` を押します。

### Android
Chromeで公開URLを開き、
メニュー → `アプリをインストール` または `ホーム画面に追加` を押します。

## データについて

予定は端末のブラウザ領域に保存されます。
ブラウザのサイトデータを削除した場合、予定も消えます。
現時点ではクラウド同期やバックアップ機能はありません。


## v2.2 修正版

- Mac用起動ファイル `予定メモを起動.command` を再同梱
- Windows用起動ファイル `予定メモを起動.bat` を再同梱
- `.github/workflows/pages.yml` と `.nojekyll` を再同梱
- スマホ追加手順・GitHub Pages公開手順を再同梱


## v2.3

- 日付欄の横幅を少し削り、時間欄の横幅を少し拡大
- 日付/時間の段の左右余白を少しだけ圧縮
- 時間欄で「分」が欠けないように調整
